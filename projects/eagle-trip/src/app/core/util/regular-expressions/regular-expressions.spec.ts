import { RegularExpressions } from './regular-expressions';

describe('RegularExpressions', () => {
  it('should create an instance', () => {
    expect(new RegularExpressions()).toBeTruthy();
  });
});
